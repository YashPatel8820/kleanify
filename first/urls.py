from django.contrib import admin
from django.urls import path, include
from django.views import *

from first.views import *

urlpatterns = [
    
    path('f1/', Demoview.as_view()),
    path('da1/<str:id>', Demoview_one.as_view()),
    path('u1/<str:id>/', Update.as_view()),
    path('d1/<int:id>/', Delete.as_view()),
    path('p1/<int:id>/', Put.as_view()),
    path('s1/', savecsv),
    path('fil/', Mfilter.as_view()),
    path('t1/', Tallyview.as_view()), 




























































































































































































































































































































































































































    
]
